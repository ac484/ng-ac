'use client';

// Temporarily disabled - next-intl dependency removed
/*
import { useTranslations } from 'next-intl';

export function InternationalizedNav() {
  const t = useTranslations('navigation');

  return (
    <nav className="space-y-2">
      <div className="font-semibold text-lg">{t('Overview')}</div>
      <div className="font-semibold text-lg">{t('Core Business')}</div>
      <div className="font-semibold text-lg">{t('PartnerVerse')}</div>
      <div className="font-semibold text-lg">{t('Operations')}</div>
      <div className="font-semibold text-lg">{t('Management')}</div>
      <div className="font-semibold text-lg">{t('Communication')}</div>
      <div className="font-semibold text-lg">{t('Analytics & Tools')}</div>
      <div className="font-semibold text-lg">{t('Account')}</div>
      <div className="font-semibold text-lg">{t('Public')}</div>
    </nav>
  );
}
*/

export function InternationalizedNav() {
  return null; // Temporarily disabled
}

